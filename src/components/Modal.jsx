const Modal = ({modal}) => {

    return (
        <>
            {modal && (
                <div className='modal'>
                    <h1>HOLA!</h1>
                </div>
            )}
        </>
    )
}

export default Modal